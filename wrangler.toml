# Local Pages Functions development:
#   npm run build
#   npx wrangler pages dev dist
#
# Production/preview deploys use the Cloudflare Pages Git integration,
# which builds the app and publishes the root `functions/` directory automatically.

name = "codestreamcoza-website"
pages_build_output_dir = "dist"

[vars]
CONTACT_TO = "info@codestream.co.za"
CONTACT_FROM = "CodeStream Website <noreply@codestream.co.za>"
